pipeline {
   agent any
   stages {
    stage('Checkout') {
      steps {
        script {
           git branch: 'ahmedtyb', 
           url: 'https://github.com/tbswahmed/LCProject.git'
          }
       }
    }
  }
}
